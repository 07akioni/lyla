{
  "name": "lyla-monorepo",
  "private": true,

  "scripts": {
    "check-type": "pnpm run -r check-type",
    "bump": "pnpm -r exec bump"
  },
  "devDependencies": {
    "version-bump-prompt": "^6.1.0"
  }
}
